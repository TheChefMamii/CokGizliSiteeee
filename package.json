{
  "dependencies": {
    "axios": "^1.6.0",
    "@netlify/functions": "^2.6.0" 
  },
  "engines": {
    "node": "18"
  },
  "scripts": {          
    "build": "echo 'No build command needed for static HTML'" 
  }
}
